name: CodeQL

permissions: {}

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    # Catches advisories published after the last commit.
    - cron: "24 5 * * 1"

concurrency:
  group: codeql-${{ github.ref }}
  cancel-in-progress: true

jobs:
  codeql:
    name: CodeQL
    uses: zemd/js/.github/workflows/shared-codeql.yml@bbc45ffa5ad42491dc7252ef798ecf1e78e9998b # v2
    permissions:
      contents: read # checkout the repository for analysis
      actions: read # let CodeQL read workflow-run metadata
      security-events: write # upload results to code scanning
    # with:
    #   languages: '["actions", "javascript-typescript"]'
